<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.corptia.bringero">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <activity
            android:name=".view.MapWork.MapsActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.locations.LocationsActivity"></activity>
        <activity
            android:name=".view.cart.CartActivity"
            android:windowSoftInputMode="stateHidden"></activity>
        <activity android:name=".view.notification.NotificationActivity" />
        <activity android:name=".view.Setting.language.LanguageActivity" />
        <activity android:name=".view.Setting.contactUs.ContactUsActivity" />
        <activity android:name=".view.Setting.changePassword.ChangePasswordActivity" />
        <activity android:name=".view.Setting.editProfile.EditProfileActivity" />
        <activity android:name=".view.Setting.main.SettingActivity" />
        <activity android:name=".view.productDetail.ProductDetailActivity" />
        <activity android:name=".view.brandDetail.BrandDetailActivity" />
        <activity android:name=".view.brands.BrandsActivity" />
        <activity
            android:name=".view.home.HomeActivity"
            android:label="@string/title_activity_store_types"
            android:theme="@style/AppTheme.NoActionBar">

        </activity>
        <activity
            android:name=".view.Main.otp.VerifyPhoneNumberActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".view.splash.SplashActivity" />
        <activity
            android:name=".view.Main.MainActivity"
            android:windowSoftInputMode="stateHidden" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />
    </application>

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

</manifest>